import React from 'react'

export default function OrderList(props) {

    return(
        <div>
            <h3>Order</h3>
            {props.map(or => {
                return
            })}
        </div>
    )


}